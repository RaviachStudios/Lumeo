# Godot 4+ specific ignores
.godot/
/android/

# Firebase admin credentials (tools/purge_expired.js). Full-admin keys — never commit.
serviceAccount*.json
*-firebase-adminsdk-*.json
node_modules/

# Firebase CLI deploy cache — regenerated on every `firebase deploy`.
.firebase/

# Build artifacts (too large for GitHub; keep local only)
Lumeo.apk
Lumeo.aab
*.apk
*.aab

# Unity Ads plugin build (source is tracked; outputs and machine-local paths are not)
plugin_unityads/build/
plugin_unityads/.gradle/
plugin_unityads/local.properties
plugin_unityads/libs/*.aar

# Trademark remediation working documents (the Hasbro memo, the remediation
# brief and the complaint log). They now live outside the repo, in
# Documents/Simon — this repo is public, so keep them out of it if they ever
# get dropped back in here.
TRADEMARK_MEMO.html
LUMEO_REBRAND_BRIEF.md
❯ Hi Developers at Raviach - claude session.txt

# Firestore exports and the rebrand migration's plan/backup files
# (tools/rebrand_migrate.js). Real player data — names, balances, inventories —
# so it stays on the machine that ran the export, never in the repo.
firestore_snapshot/
